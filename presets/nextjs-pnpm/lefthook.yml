pre-commit:
  parallel: true
  commands:
    typecheck:
      run: pnpm exec tsc --noEmit

    tests:
      run: pnpm test

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}
